const submitForm = () => {
    var booking_id = document.getElementById("booking_id").value;
    var slot_description = document.getElementById("slot_description").value;
    var sku = document.getElementById("sku").value;
    console.log("booking_id: " + booking_id + " slot_description: " + slot_description + " sku: " + sku);
    // and just like that you have control of the data
}